flatbuffers==2.0.0
matplotlib==3.7.1
opencv-contrib-python==4.8.1.78
psutil==5.9.4
pyautogui==0.9.53
customtkinter==5.2.0
mediapipe==0.9.3.0
PyDirectInput==1.0.4;  sys_platform == 'win32'
pywin32==306;  sys_platform == 'win32'
pygrabber;  sys_platform == 'win32'
pyinstaller==5.11.0
Pillow==10.1.0
numpy==1.26.2
#
# Library with a cross-platform interface for determining where to store app
# data, see https://pypi.org/project/platformdirs/
platformdirs==4.2.0
#
# HTTP request library whose client handles the redirects and such from the
# GitHub asset retrieval API, see https://pypi.org/project/requests/
requests==2.31.0
#
# Library for human readable dates, times, and file sizes, see
# https://pypi.org/project/humanize/
humanize==4.9.0
